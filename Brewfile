# Homebrew Bundle - System Dependencies for run CLI
# Run `run bundle` to install all dependencies

tap "homebrew/bundle"

# Used by: run agent (git worktree management)
brew "git"

# Used by: run yt download
brew "yt-dlp"

# Used by: run convert video
brew "ffmpeg"

# Used by: run db password
brew "awscli"
brew "aws-sso-util"
